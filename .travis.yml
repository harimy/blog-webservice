language: java
jdk:
  - openjdk8

branches:
  only:
    - main

# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.maven'

script: "./mvnw clean package"

before_deploy:
  - zip -r blog-webservice *
  - mkdir -p deploy
  - mv blog-webservice.zip deploy/blog-webservice.zip

deploy:
  - provider: s3
    access_key_id: $AWS_ACCESS_KEY # Travis repo settings에 설정된 값
    secret_access_key: $AWS_SECRET_KEY # Travis repo settings에 설정된 값
    bucket: rimha-blog-build  # S3 버킷
    region: ap-northeast-2
    skip_cleanup: true
    acl: private # zip 파일 접근을 private으로 설정
    local_dir: deploy # before_deploy에서 생성한 디렉토리
    wait-unitl-deployed: true
    on:
      all_branches: true

  - provider: codedeploy
    access_key_id: $AWS_ACCESS_KEY
    secret_access_key: $AWS_SECRET_KEY
    bucket: rimha-blog-build
    key: blog-webservice.zip  # 빌드 파일을 압축해서 전달
    bundle_type: zip # 압축 확장자
    applicaiton: rimha-blog # 웹 콘솔에서 등록한 CodeDeploy 애플리케이션
    deployment_group: rimha-blog-group # 웹 콘솔에서 등록한 CodeDeploy 배포 그룹
    region: ap-northeast-2
    wait-until-deployed: true
    on:
      all_branches: true

# CI 실행 완료 시 메일로 알림
notifications:
  email:
    recipients:
      - harimlee1022@gmail.com